{
  "description": "Analyzer for BR-06 Water Activity",
  "baseAnalyzerId": "prebuilt-documentAnalyzer",
  "config": {
    "returnDetails": true,
    "enableFormula": false,
    "disableContentFiltering": false,
    "estimateFieldSourceAndConfidence": true,
    "tableFormat": "html"
  },
  "fieldSchema": {
    "fields": {
      "Supplier": { "type": "string", "method": "extract", "description": "Laboratory or supplier name issuing the water activity report (header/footer)." },
      "MaterialName": { "type": "string", "method": "extract", "description": "Material name tested for water activity." },
      "MaterialCode": { "type": "string", "method": "extract", "description": "Material identifier or code, if present on the report." },
      "Lot": { "type": "string", "method": "extract", "description": "Batch/lot identifier for the tested sample." },
      "ReportDate": { "type": "string", "method": "extract", "description": "Report date or test date associated with the result." },
      "WaterActivity": { "type": "number", "method": "extract", "description": "Water activity (aw) numeric value. Strip labels like 'aw' and capture the decimal number (e.g., 0.420)." },
      "TemperatureC": { "type": "number", "method": "extract", "description": "Temperature in °C at which aw was measured (e.g., 25). If stated as a range, prefer the target/nominal test temperature." },
      "Method": { "type": "string", "method": "extract", "description": "Test method/standard used (e.g., ISO 18787)." },
      "Instrument": { "type": "string", "method": "extract", "description": "Instrument/model used for aw measurement (e.g., Aqualab 4TE)." }
    }
  }
}
